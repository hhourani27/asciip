import { Typography } from "@mui/material";

export function Logo(): JSX.Element {
  return (
    <Typography sx={{ fontFamily: "MartianMono", fontWeight: 600 }}>
      AsciiP
    </Typography>
  );
}
